using UnityEngine;

/// <summary>
/// The Main Behaviour of a Block GameObject
/// </summary>
public class BlockBehaviour : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
